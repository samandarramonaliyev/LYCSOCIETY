import type {ButtonHTMLAttributes,InputHTMLAttributes,ReactNode} from "react";
export function Button({variant="primary",loading=false,children,...p}:{variant?:"primary"|"secondary"|"ghost"|"danger";loading?:boolean;children:ReactNode}&ButtonHTMLAttributes<HTMLButtonElement>){return <button {...p} disabled={p.disabled||loading} className={`min-h-11 rounded-md px-4 text-sm font-semibold transition focus-visible:outline ${variant==="primary"?"bg-burgundy text-white hover:brightness-110":variant==="danger"?"bg-[var(--danger)] text-white":variant==="secondary"?"border border-[var(--border)] bg-paper text-ink":"text-burgundy hover:bg-paper"}`}>{loading?"Working…":children}</button>}
export function Card({children,className=""}:{children:ReactNode;className?:string}){return <section className={`border border-[var(--border)] bg-paper p-5 shadow-[0_2px_10px_rgba(36,31,26,.04)] ${className}`}>{children}</section>}
export function PageHeader({eyebrow,title,description}:{eyebrow?:string;title:string;description?:string}){return <header className="mb-7"><div className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-[var(--accent)]">{eyebrow}</div><h1 className="font-serif text-4xl leading-tight text-[var(--ink-dark)]">{title}</h1>{description&&<p className="mt-2 max-w-xl text-[var(--muted)]">{description}</p>}</header>}
export function State({kind,title,body,action}:{kind:"loading"|"error"|"empty";title:string;body?:string;action?:ReactNode}){return <Card className="text-center"><div className="font-serif text-xl">{kind==="loading"?"Preparing your society":title}</div>{body&&<p className="mt-2 text-sm text-[var(--muted)]">{body}</p>}{action&&<div className="mt-4">{action}</div>}</Card>}
