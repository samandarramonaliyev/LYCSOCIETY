import {NavLink,Outlet,useLocation} from "react-router-dom"; import {Bell,Compass,Home,UserRound,Shield} from "lucide-react";
const items=[{to:"/",label:"Home",icon:Home},{to:"/discover",label:"Discover",icon:Compass},{to:"/my-club",label:"My Club",icon:Shield},{to:"/profile",label:"Profile",icon:UserRound}];
export function Shell(){const loc=useLocation(); return <div className="min-h-screen pb-24"><main className="mx-auto min-h-screen w-full max-w-2xl px-5 pb-8 pt-[max(1.25rem,env(safe-area-inset-top))]"><div className="mb-8 flex items-center justify-between border-b border-[var(--border)] pb-4"><div><div className="font-serif text-2xl tracking-tight">LYC Society</div><div className="text-[10px] font-bold uppercase tracking-[.22em] text-[var(--muted)]">A student society</div></div><NavLink aria-label="Notifications" to="/notifications" className={loc.pathname==="/notifications"?"text-burgundy":"text-[var(--muted)]"}><Bell size={20}/></NavLink></div><Outlet/></main><nav className="fixed inset-x-0 bottom-0 z-10 border-t border-[var(--border)] bg-paper/95 pb-[env(safe-area-inset-bottom)] backdrop-blur"><div className="mx-auto flex max-w-2xl justify-around py-2">{items.map(({to,label,icon:Icon})=><NavLink key={to} to={to} className={({isActive})=>`flex min-h-12 min-w-16 flex-col items-center justify-center gap-1 text-[11px] ${isActive?"font-semibold text-burgundy":"text-[var(--muted)]"}`}><Icon size={19} strokeWidth={1.7}/>{label}</NavLink>)}</div></nav></div>}
